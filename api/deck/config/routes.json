{
  "routes": [
    {
      "method": "GET",
      "path": "/decks",
      "handler": "deck.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/decks/count",
      "handler": "deck.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/decks/:id",
      "handler": "deck.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/decks",
      "handler": "deck.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/decks/:id",
      "handler": "deck.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/decks/:id",
      "handler": "deck.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
